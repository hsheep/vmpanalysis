IDA VMProtect 3.4 Analysis Script

    通过 Unicorn engine 模拟分析每个指令块的地址，再通过花指令消减和VM指令匹配，来静态分析提取虚拟化的指令

* 已经完成:
    (1) x86寄存器传播分析，指令优化 [ok]
    * jcc指令分析处理 [ok]
    * xchg指令处理有问题 [ok]
    x x86指令化简

    (2) VM代码识别
    * VM指令识别 [ok]
    * 伪代码模式匹配 [ok]
    * VM寄存器适配 [ok]

* 待加入逻辑
    * 没有对段寄存器进行标注
    * 没有对操作数宽度进行标注
    * 推算寄存器索引的解密算法和vEip的解密算法

* 待修复问题：
    * 解决在提取指令流的时候，会出现与动态执行不一致导致的VM指令内存异常。 [doing]

* 和接下来要做的事情：
    1. VM指令化简，将VM指令标注成可读性强的x86类指令。
    2. 通过capstone汇编引擎将虚拟指令转化为x86指令。
    3. 虚拟寄存器映射物理寄存器，去除虚拟机。