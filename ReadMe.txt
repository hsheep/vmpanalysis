IDA VMProtect 3.4 Analysis Script

    通过 Unicorn engine 模拟分析每个指令块的地址，再通过花指令消减和VM指令匹配，来静态分析提取虚拟化的指令

* 已经完成:
    第一步：
    (1) x86寄存器传播分析，指令优化 [ok]
    * jcc指令分析处理 [ok]
    * xchg指令处理有问题 [ok]
    x x86指令化简

    (2) VM代码识别
    * VM指令识别
    * 伪代码模式匹配
    * VM寄存器适配


* 和接下来要做的事情：
    1. 解决在提取指令流的时候，会出现与动态执行不一致导致VM指令内存地址异常。
    2. VM指令化简，将VM指令标注成可读性强的x86类指令。
    3. 通过capstone汇编引擎将虚拟指令转化为x86指令。
    4. 虚拟寄存器映射物理寄存器，去除虚拟机。